# Publishers

!!! warning "Status: Spec — section under construction"
    The Publishers section is being authored. It will document the generic install pattern for SDK v3 publishers and the catalog of available collectors (Prometheus, IPMI, NVML, PMU, Slurm, GPU, MQTT bridge).

> The Publishers sub-section is for sysadmins installing collectors on individual nodes: master nodes, compute nodes, BMC hosts. It covers the generic SDK v3 install pattern (pip + systemd + MQTT) and a thin landing page per available publisher that links to the publisher's component README.

---

## Source

- IA defined in `Design_by_Documentation_Method_May_2026.md` §6.3.
