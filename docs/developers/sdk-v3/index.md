# SDK v3

!!! warning "Status: Spec — section under construction"
    The SDK v3 section is being authored. The quickstart, concepts, workers reference, and configuration pages land in subsequent slices. The example walkthrough for the legacy `examon_pub` notebook is already reachable from the section nav as a starting point.

> The SDK v3 sub-section documents the publisher framework used to write a new ExaMon collector. The framework provides workers (extractors, transformers, loaders), a lifecycle, and a YAML configuration model; a typical publisher is a few lines of Python wiring those pieces together.

---

## Source

- IA defined in `Design_by_Documentation_Method_May_2026.md` §6.3.
