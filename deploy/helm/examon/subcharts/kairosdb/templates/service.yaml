apiVersion: v1
kind: Service
metadata:
  name: {{ include "kairosdb.fullname" . }}
  labels:
    {{- include "kairosdb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kairosdb.selectorLabels" . | nindent 4 }}
